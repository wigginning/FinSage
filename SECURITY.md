# 安全公告（SECURITY）

FinSage 高度重视安全问题。感谢你帮助我们维护项目的安全性。

## 支持的版本

| 版本 | 支持状态 |
|------|----------|
| `master` 主线 | ✅ 活跃维护 |

仅 `master` 分支持续维护。旧 tag 不保证安全修复。请始终基于最新主线构建与部署。

## 报告方式

请**不要**在 GitHub Issue 或公开渠道提交安全漏洞详情，这会令仍受影响的其他用户暴露于风险中。

请将漏洞以**私密**方式报告给维护者：

- 首选：**GitHub Security Advisory / 私密漏洞报告**（仓库首页 → Security → Report a vulnerability）；
- 或：通过项目维护者登记的私密联系渠道（见仓库 README 维护者信息）。

### 报告时应包含

- 复现步骤（尽量最小化、可独立运行）；
- 受影响版本 / 分支；
- 漏洞类型（如注入、越权、信息泄露、认证绕过等）；
- 预期与实际行为差异；
- 如可行，附带 PoC。

如内容包含密钥或高风险凭据，请先在本地进行脱敏再提交，勿在流量中明文传输原始凭据。

## 处理流程

1. **确认**：维护者在收到报告 72 小时内回复确认；
2. **评估**：判断影响面与严重性（CVSS）；
3. **修复与披露**：修复后发布补丁，并在 `docs/CHANGELOG.md` 记录；如涉及已发布版本，优先发布安全修复版后再公开细节（协调披露）；
4. **致谢**：经你同意后在新版本/公告中致谢。

## 安全设计基线（本仓库既定约束）

- **密钥永不入库**：仅 `.env.example` / `.env.local.example` 样例入库；真实密钥一律经环境变量/配置注入（AGENTS.md §7）。
- **审计日志脱敏**：`secret` 永不写入 `audit_events`；所有入参出参摘要经 `mask_value` 脱敏（§16.15 / §25）。
- **外部内容不可信**：外部文档/网页视为不可信输入；检索证据默认按纯文本渲染，不允许被检索内容覆盖系统指令。
- **确定性优先**：财务数字仅由确定性代码计算，禁止 LLM 执行权威财务运算（AGENTS.md §3）。

## 依赖与漏洞告警

请定期检查以下仓库的安全告警与依赖更新：

- Python：`pip-audit`（Github Actions 与本地均可启用）
- 前端：`npm audit`
- Docker 镜像：定期重建以获取基础镜像安全补丁

## 常见问题（FAQ）

- **我在 `.env` 里填了真实密钥，是否安全？** `.env` 已被 `.gitignore` 忽略、不随仓库分发；但任何真实密钥一旦可能在任意环节暴露（剪贴板、会话、第三方工具链），应立即在对应服务后台吊销并轮换。**永远不要**把 `.env` 内容粘贴到 Issue/聊天。
- **MinIO / MySQL 的默认口令能用吗？** 不行。`finsage` / `minioadmin` 等仅为本地一键启动的 dev 默认值，生产环境必须通过环境变量注入强随机口令并禁止默认值。

## 开源许可证

本项目以 Apache-2.0 发布，详见 [`LICENSE`](LICENSE)。